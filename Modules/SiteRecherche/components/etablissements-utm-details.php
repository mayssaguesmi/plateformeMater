<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Faculté de médecine de Tunis</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* General body styling */
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8f9fa;
        }

        /* Custom Red Color */
        .text-custom-red {
            color: #b60303;
        }

        .bg-custom-red {
            background-color: #b60303;
        }

        /* Hero section styling */
        .hero-bg {
            /* Using a placeholder for the new background image */
            background-image: url('/wp-content/plugins/plateforme-master/images/SiteRechercheImages/Groupe 3330 (1).png');
            background-size: cover;
            background-position: center;
            padding: 6rem 0;
            color: white;
        }

        .breadcrumb-custom {
            background-color: rgb(83 81 81 / 40%);
            display: inline-block;
            padding: 0.5rem 1rem;
            border-radius: 0.25rem;
            margin-bottom: 1rem;
            font-size: 0.9rem;
        }

        .breadcrumb-custom a {
            color: white;
            text-decoration: none;
        }

        .breadcrumb-custom a:hover {
            text-decoration: underline;
        }

        .breadcrumb-custom span {
            color: #e9ecef;
            margin: 0 0.5rem;
        }

        .hero-title {
            font-weight: 700;
            font-size: 3.5rem;
        }

        /* New Title Card Styling */
        .title-card {
            background-color: white;
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 1px 1px 14px 1px rgba(0, 0, 0, 0.15);
            /* border: 1px solid #dee2e6; */
            margin-top: -5rem;
            position: relative;
            z-index: 10;
            margin-bottom: 2rem;

            /* Space between title and content cards */
        }

        .title-card h2 {
            font-weight: 700;
            font-size: 1.75rem;
            margin-bottom: 0.5rem;
            text-align: center;
        }

        .title-card .location {
            color: #6c757d;
            font-size: 1rem;
            margin-bottom: 0;
            margin-inline: 165px;
            display: flex;
            align-items: center;
        }

        .title-card .location i {
            color: #b60303;
            margin-right: 0.5rem;
        }


        /* Main Content Styling */
        .content-card {
            background-color: white;
            padding: 3rem;
            border-radius: 1rem;
            box-shadow: 1px 1px 14px 1px rgba(0, 0, 0, 0.15);
            /* border: 1px solid #dee2e6; */
            position: relative;
            margin-bottom: 80px;
        }

        .content-card .content-image {
            width: 100%;
            border-radius: 0.75rem;
            margin-bottom: 2.5rem;
        }

        .content-card h3 {
            font-weight: 600;
            font-size: 1.5rem;
            margin-bottom: 1rem;
            /* border-left: 4px solid #b60303; */
            /* padding-left: 1rem; */
        }

        .content-card p {
            line-height: 1.8;
            color: #000000ff;
            font-weight: 500;
        }

        /* Styles from the original file (kept as requested) */
        .search-box {
            background-color: white;
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.05);
            border: 1px solid #dee2e6;
        }

        .publication-card {
            background-color: #ffffff;
            border-radius: 1rem;
            padding: 1.5rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
            height: 100%;
            position: relative;
            overflow: hidden;
            border: none;
        }
    </style>
</head>

<body>

    <!-- Hero Section -->
    <section class="hero-bg">
        <div class="container">
            <p class="breadcrumb-custom">
                <a href="#">Université de Tunis El Manar</a><span>›</span> <a href="#">Structures de recherche
                </a><span>›</span> Etablissements
            </p>
            <h1 class="hero-title">Faculté de médecine <br> de Tunis</h1>
        </div>
    </section>

    <!-- Main Content -->
    <main>
        <div class="container">
            <div class="title-card">
                <h2>Ecole Supérieure des Sciences et Techniques de la Santé de Tunis</h2>
                <p class="location">
                    <!-- <i class="fa-solid fa-map-marker-alt"></i> -->

                    <img width="15px" class="me-2"
                        src="/wp-content/plugins/plateforme-master/images/SiteRechercheImages/27) Icon-pin.png"
                        alt="Icon-pin">

                    BP n° 176 Bab Souika - 1007 Tunis
                </p>
            </div>
            <div class="content-card">
                <!-- Placeholder image for the school -->
                <img src="/wp-content/plugins/plateforme-master/images/SiteRechercheImages/Groupe 3330.png"
                    alt="Ecole Supérieure des Sciences et Techniques de la Santé de Tunis" class="content-image">

                <h3>Résumé</h3>
                <p>
                    Fondée vers la fin des années soixante (loi N° 4161 du 31/12/1968), l'ENIT, est la doyenne des
                    écoles d'ingénieurs de Tunisie. Elle n'a cessé d'être investie de missions nationales déterminantes.
                    De même, elle a essaimé durant les années quatre vingt certaines des plus jeunes écoles d'ingénieurs
                    enrichissant aujourd'hui le panorama tunisien de la formation d'ingénieurs. L'ENIT a été, enfin, et
                    continue d'être, un vivier renouvelable d'experts et de hauts cadres de l'Etat, de compétences
                    reconnues dans les domaines de l'enseignement supérieur, de la recherche et de l'ingénierie.
                </p>
            </div>
        </div>
    </main>

    <!-- Bootstrap 5 JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Original script kept as requested -->
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Note: The elements targeted by this script (e.g., 'domainSelect', 'publicationsContainer')
            // do not exist on this page, so this script will not have any effect.
            // It is kept here to fulfill the original request.
            const domainSelect = document.getElementById('domainSelect');
            const keywordsSelect = document.getElementById('keywordsSelect');
            const applyBtn = document.getElementById('applyBtn');
            const resetBtn = document.getElementById('resetBtn');
            const publicationsContainer = document.getElementById('publicationsContainer');
            const publicationItems = publicationsContainer ? publicationsContainer.querySelectorAll(
                '.publication-item') : [];
            const noResultsMessage = document.getElementById('noResultsMessage');

            function filterPublications() {
                if (!publicationsContainer) return; // Guard clause if elements don't exist

                const selectedDomain = domainSelect.value;
                const selectedKeyword = keywordsSelect.value;
                let visibleCount = 0;

                publicationItems.forEach(item => {
                    const itemDomains = item.dataset.domain.split(',');
                    const itemKeywords = item.dataset.keywords.split(',');

                    const domainMatch = !selectedDomain || itemDomains.includes(selectedDomain);
                    const keywordMatch = !selectedKeyword || itemKeywords.includes(selectedKeyword);

                    if (domainMatch && keywordMatch) {
                        item.style.display = '';
                        visibleCount++;
                    } else {
                        item.style.display = 'none';
                    }
                });

                if (noResultsMessage) {
                    noResultsMessage.style.display = visibleCount === 0 ? 'block' : 'none';
                }
            }

            if (applyBtn) {
                applyBtn.addEventListener('click', filterPublications);
            }

            if (resetBtn) {
                resetBtn.addEventListener('click', () => {
                    domainSelect.value = '';
                    keywordsSelect.value = '';
                    filterPublications();
                });
            }
        });
    </script>

</body>

</html>